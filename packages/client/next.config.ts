import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  // Choose one of your route prefixes as the basePath
  basePath: '/next',
  // This helps with asset loading
  assetPrefix: '/next',
}

export default nextConfig;
